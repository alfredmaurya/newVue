export const themeOptions = (state) => state.themeOptions

export const themeApplied = (state) => state.themeApplied

export const enterClass = (state) => state.enterClass

export const leaveClass = (state) => state.leaveClass

export const locations = (state) => state.locations

export const employees = (state) => state.employees

export const roles = (state) => state.roles

export const facilitys = (state) => state.facilitys

export const customers = (state) => state.customers

export const accommodation = (state) => state.accommodation

export const accommodationType = (state) => state.accommodationType
